import { JrSwitchCasesDirective } from './jr-switch-cases.directive';

describe('JrSwitchCasesDirective', () => {
  it('should create an instance', () => {
    const directive = new JrSwitchCasesDirective();
    expect(directive).toBeTruthy();
  });
});
